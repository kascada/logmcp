---
- name: restart logmcp
  ansible.builtin.systemd:
    name: logmcp
    state: restarted

- name: reload fail2ban
  ansible.builtin.command:
    cmd: fail2ban-client reload
  ignore_errors: true
  changed_when: false
